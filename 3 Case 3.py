# Case 3: Defining 2 diff variables with same values then only one object is
#         Created and that object address returned to both the variables


x=10

y=10

print(x, id(x), type(x))
print(y, id(y), type(y))
